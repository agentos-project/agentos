# These dependencies are also listed in the <proj_root>/dev-requirements.txt
# If you change these, make the same changes there.

-e ../../
tensorflow==2.2.2  # reinforce, dqn
tensorflow-probability==0.10.0  # reinforce
pandas>=1.1.5,<=1.2.0 # dqn. 1.2.0 not available on python 3.6
